{
  "name": "Validate Models on Save",
  "description": "Automatically run model tests when data models or scoring tools are modified",
  "trigger": {
    "type": "file_save",
    "pattern": "src/{models,tools_adk,evaluation}.py"
  },
  "actions": [
    {
      "type": "run_command",
      "command": "python -m pytest tests/test_models.py tests/test_tools.py -v",
      "description": "Validate data models and scoring tools"
    },
    {
      "type": "ai_task",
      "prompt": "Review the saved file for EU AI Act compliance patterns. Check if any risk tier classifications or scoring logic needs updating based on the changes.",
      "context_files": [
        "src/models.py",
        "src/tools_adk.py",
        "src/evaluation.py"
      ]
    }
  ],
  "enabled": true
}
